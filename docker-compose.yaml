version: "3.9"
services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: "postgres"
      POSTGRES_USER: "pguser"
      POSTGRES_PASSWORD: "pgpwd4pguser"
    ports:
      - "5432:5432"